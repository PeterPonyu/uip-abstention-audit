---
layout: base.njk
title: Controls
permalink: /controls/
---

# Nulls and failures first

Clean shuffle, a falsified knapsack allocator, and failed temporal transfer are evidence that bounds the interpretation. They are not a limitations appendix.

<figure class="figure">
  <img src="{{ '/assets/web/w6_shuffle.svg' | url }}" alt="Real prototype-blocked cells exclude zero in 45 of 60; shuffle is 0 of 60.">
  <figcaption>Real versus within-stratum shuffle. Prototype-blocked real cells exclude zero in 45 of 60; the shuffle null is 0 of 60.</figcaption>
</figure>

<figure class="figure">
  <img src="{{ '/assets/web/w5_loeo.svg' | url }}" alt="Leave-one-element-out bars; oxygen is highlighted because it removes the oxide stratum.">
  <figcaption>Leave-one-element-out. All 12 splits pass the pre-registered robustness rule. Dropping oxygen removes the oxide stratum rather than “winning.”</figcaption>
</figure>

<figure class="figure">
  <img src="{{ '/assets/web/w9_scope.svg' | url }}" alt="Four scope cards: labels, allocator, temporal transfer, and effect-size reading.">
  <figcaption>Scope with the same typographic weight as the positive cells. A stratum-aware allocator is falsified. The oxide-highest ordering does not transfer temporally.</figcaption>
</figure>

## What remains standing

- The oxide-localized matched-yield interaction on the four UIPs.
- The 32-UIP oxide anchor with no generational difference (<em>p</em> = 0.68).
- WBM-round majority robustness, with the honest splits that fail kept visible.

<p><a href="{{ '/protocol/' | url }}">Protocol</a> · <a href="{{ '/reproduce/' | url }}">Reproduce</a></p>
